# Handoff Notes

## 現在の状態

このリポジトリは、Packet Tracer 風のネットワーク到達性シミュレータの MVP を実装中。

現時点では以下まで完了している。

- Laravel 11 + Inertia + React + TypeScript + React Flow の開発土台
- Docker Compose ベースの最小ローカル実行環境
- `NetworkProject` / `Device` / `NetworkCloud` / `DeviceInterface` / `Link` / `RouteEntry` の永続化モデル
- トポロジー保存 / 再読込 API
- Ping シミュレーション API の初版
- React Flow 上でのノード追加、削除、ドラッグ移動、接続
- ノード編集モーダル
- 右クリックメニューによる機器追加 / ノード削除
- Ping 結果表示
- UI の主要文言の日本語化

ローカル `main` は `origin/main` を取り込んだ状態で、現在の HEAD は `e6b1184`。

## 主要ファイル

### バックエンド

- `app/Http/Controllers/NetworkProjectController.php`
- `app/Http/Controllers/PingSimulationController.php`
- `app/Services/NetworkProjectTopologyService.php`
- `app/Services/NetworkSimulator/PingSimulator.php`
- `routes/api.php`

### フロントエンド

- `resources/js/Pages/NetworkEditor.tsx`
- `resources/js/Components/TopologyNode.tsx`
- `resources/js/Types/network.ts`
- `resources/css/app.css`

### ドキュメント / 実行環境

- `README.md`
- `docs/local-development.md`
- `docs/architecture.md`
- `docker-compose.yml`
- `docker/php/Dockerfile`
- `docker/php/start-app.sh`
- `docker/php/start-vite.sh`

## 現在できること

- プロジェクト名 / 説明の編集
- PC / Switch / Router / Firewall / Internet / Master'sONE ノードの追加
- ノードの右クリックメニュー操作
- ノード左クリックからのモーダル編集
- Switch を 8 ポートで追加
- Switch の L2 / L3 種別切替
- ノード同士のリンク追加 / 削除
- 構成保存 / 再読込
- PC / Cloud を対象にした Ping シミュレーション
- 成功 / 失敗理由 / hop / suggestion の表示

## 現在の制約

- `PingSimulator` は next-hop を再帰的にたどる構成にはまだなっていない
- Master'sONE 判定も厳密な経路探索ではなく、MVP 寄りの簡略実装
- `NetworkEditor.tsx` がかなり大きく、責務が集中している
- `resources/js/Types/network.ts` の型は、まだ一部を union 化できる
- `docker/node/Dockerfile` まわりは整理余地あり

## 直近レビューで残ったメモ

次 PR で対応候補になっているもの:

1. `NetworkEditor.tsx` の分割
2. `PingSimulator` の分割
3. `next_hop` 再帰探索
4. `docs/handoff-2026-06-28.md` の更新または整理
5. `docker/node/Dockerfile` と Vite 起動スクリプト配置の整理

## 次にやること

次の優先作業はフロント整理。

目的:

- `NetworkEditor.tsx` の肥大化を解消する
- UI 改善を続けやすい構造にする
- ノード編集、キャンバス、Ping パネル、ツールバー、コンテキストメニューの責務を分離する

候補コンポーネント:

- `TopologyCanvas`
- `ProjectToolbar`
- `NodeEditorModal`
- `PingPanel`
- `ProjectPanel`
- `TopologyContextMenu`

次のブランチでまずやるべきこと:

1. `NetworkEditor.tsx` から表示専用 UI を切り出す
2. 状態更新ロジックを小さな helper に寄せる
3. `DeviceType` / `NetworkCloudType` の union 型を導入する
4. UI 挙動を壊していないことを `npm run build` で確認する

## 動作確認コマンド

### フロントビルド

```bash
npm run build
```

### バックエンドテスト

```bash
docker compose exec -T app php artisan test
```

## 再開時に最初に見るファイル

- `docs/handoff-2026-06-28.md`
- `docs/roadmap.md`
- `resources/js/Pages/NetworkEditor.tsx`
- `resources/js/Types/network.ts`
- `app/Services/NetworkSimulator/PingSimulator.php`
